{
  "edge_type": "AddKeyCredentialLink",
  "description": "Write msDS-KeyCredentialLink (Shadow Credentials) to obtain a certificate for PKINIT auth as the target.",
  "static_noise_score": 35,
  "telemetry": [
    {
      "source": "windows_security",
      "event_id": 5136,
      "detail": "msDS-KeyCredentialLink attribute write - logged only under DS-change auditing (default OFF).",
      "reliability": "high_if_auditing_enabled",
      "default_enabled": false
    },
    {
      "source": "windows_security",
      "event_id": 4768,
      "detail": "Kerberos TGT request via PKINIT (certificate) following the abuse - certificate pre-auth is an anomaly signal.",
      "reliability": "medium",
      "default_enabled": true
    },
    {
      "source": "edr_heuristic",
      "product_class": "MDI",
      "detail": "Quieter than a password reset (no state the user sees), but modern ITDR (MDI) increasingly flags KeyCredentialLink writes.",
      "reliability": "medium"
    },
    {
      "source": "wdac",
      "event_id": 3077,
      "product_class": "WDAC",
      "detail": "WDAC / App Control blocks the unsigned Whisker / pyWhisker (shadow credentials) binary on-host (enforce 3077; audit-only logs 3076). Evaded by native LOLBin or remote (Impacket) execution - see the tooling axis.",
      "reliability": "high",
      "default_enabled": false
    }
  ],
  "mitre_technique": "T1556",
  "prerequisite_privilege": "GenericWrite/GenericAll over target's msDS-KeyCredentialLink",
  "abuse_primitive": "Write key credential (Whisker/Certipy shadow-creds), request cert, PKINIT to TGT.",
  "notes": "Stealth path of choice for GenericAll/GenericWrite over accounts. Loud where DS-change auditing or MDI is present; quiet otherwise."
}
